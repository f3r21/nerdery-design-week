# Nerdery Design Week · engineering track · Vello

One week on Vello, a neighborhood services app: my pass first, Claude's second, every claim checked against its source.

| Folder | What it holds |
|---|---|
| `mon/` | Reflection: what design owns, what engineering contributes, one decision reframed as a system decision, two pushbacks. |
| `tue/` | Research synthesis (seven themes, two audited to the transcripts), two problem statements, entity list. |
| `wed/` | `post-a-request-flow.html`: flow, 89 screen-state rows, 14 endpoints, 18 gaps. Earlier drafts kept beside it as evidence. |
| `thu/` | Comparative critique of the assigned screen (neighbor detail, Grace Lin): human pass, AI pass, resolution table. |
| `fri/` | `ProviderCard.html` (self-contained, opens offline), `FIDELITY-AUDIT.md` (one page), eight open questions in `spec-by-hand.md`, the guardrail `CLAUDE.md`, the token lint in `tools/`, frozen v1 in `v1/`. |
| `demo/` | `present.html`, the 11-step walkthrough (Chrome, arrow keys, works offline); `index.html`, the whole week; `evidence.md`, every claim with its verbatim source; `deck-lint.txt`. |
| root | `handoff-checklist.md`, `engineering-design-support-checklist.md`, `where-i-corrected-claude.md`. |

Open `demo/index.html` for the week and `demo/present.html` for the 11-step walkthrough (Chrome, arrow keys). Both work offline; `demo/steps/00.png` to `10.png` are the fallback. Sources cited by path live beside this folder: `vello/` (design system and prototype) and `Vello_Interview_Transcripts/`. The two no-AI steps (`fri/spec-by-hand.md`, `thu/human-pass.md`) state on their own pages how they were made; every verdict in them is mine.
